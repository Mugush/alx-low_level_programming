#include "main.h"
/*
 * _strlen_recursion - Function that returns the length of a string.
 * @s: The integer string to be printed out.
 *
 * Return: The length of a string.
 */
int _strlen_recursion(char *s)
{
	int length = 0
	
		if (*s)
		{
			length ++;
			length + = _strlen_recusrion(s+1);

		printf("%d\n", n);
		}
		return (length);
