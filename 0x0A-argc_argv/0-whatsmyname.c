#include <stdio.h>
/**
 * main - program to print its name followed by new line.
 * @argc: the argument count.
 * @argv: the argument vector.
 * Return: 0 on success.
 */
int main(int argc, char *argv[])
{
	argc = argc;

	printf("%s\n", argv[0]);
	return (0);
}
