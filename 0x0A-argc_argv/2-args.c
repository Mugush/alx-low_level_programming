#include <stdio.h>

/**
 * main- prints all arguments it receives.
 * @argc: array size.
 * @argv: array containing arguments.
 *
 * Return: 0 on success
 */
int main(int argc, char *argv[])
{
	printf("argc=%d\n", argc);

	return (0);
}
